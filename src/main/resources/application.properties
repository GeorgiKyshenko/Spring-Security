# disables the Spring logo (banner) when we start the app
spring.main.banner-mode=off

# formatting the logging info on the console
logging.pattern.console= %d{dd-MM HH:mm:ss} - %logger{36} - %msg%n

# hardcoded credentials for Spring Security. When we create InMemoryDetailsManager, this hardcoded user is not valid!!!
spring.security.user.name=Georgi
spring.security.user.password=123

#
spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.datasource.url=jdbc:h2:mem:testdb
